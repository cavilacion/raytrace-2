example.json contains a file with spheres, a triangle, a torus, and a plane in the background.
These are all the shapes that we have implemented.
The directory screenshots, contains a drawed scene from this file.

